services:
  - type: web
    name: personal-finance-prediction
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python backend/app/main.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
      - key: PORT
        value: 10000
